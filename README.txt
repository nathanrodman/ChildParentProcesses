An assignment for Computer Systems using child and parent processes. The parent sends in characters from the command line and the child process counts the number of characters and returns that number.

Created and ran on Linux Gnome.